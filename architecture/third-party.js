export var DHTMLX = {
    Event: dhtmlxEvent,
    LayoutObject: dhtmlXLayoutObject,
    TabBar: dhtmlXTabBar
};

export var Electron = {
  remote: require('electron').remote
};

export var NodeJS = {
  fs: require('fs'),  // https://nodejs.org/api/fs.html
  path: require('path')
};

export var BigInteger = {
  bigInt: bigInt
};

export var AceEditor = {
  ace: ace
};
